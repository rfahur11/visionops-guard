apiVersion: 1

providers:
  - name: 'VisionOps Guard'
    orgId: 1
    folder: 'VisionOps MLOps'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 5
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
